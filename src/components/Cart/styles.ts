import styled from 'styled-components';

export const Container = styled.div`
    width: 3.1rem;
    
    Button {
        width: 90px;
        height: 45px;
        justify-content: space-evenly;
        
        img {
            margin-right: 1rem;
        }

        span {
            margin-right: 1rem;
            color: #000000;
            font-weight: 700;
            font-size: 18px;
        }
    }
`